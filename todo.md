# 写个一次性脚本修改文件目录和文件名:
## 1. 在代码中 hardcode 扫描的目录
## 2. 从数据库中读取文件名, 如果 match 扫描目录, 则:
 1. 把文件名改为"番号.女优_1.女优_2[标题]", 这里的番号不要省略 -C -U -UC, sample "SSIS-698-UC.三上悠亚.新有菜.相泽南.[和新有菜与相泽南 精美写真三张].mp4"
 2. 把文件移动到扫描目录/番号/, 如果没有则创建目录. 例如上面的文件需要移动到 SSIS 目录下
 3. 如果有part则加上, 例如 SSIS-698-UC.三上悠亚.新有菜.相泽南.[和新有菜与相泽南 精美写真三张].part1.mp4
 4. 一定要更新数据库

## 3. 有 dryrun 参数和 count 参数, 使用 dryrun 和 count 时, 仅仅获取 count 数的文件, 并给出预计移动的 source 和 dest, 不做真实移动
